pipeline {
    agent any

    stages {

        stage('Checkout Code') {
            steps {
                git 'https://github.com/Ida-code/Student_perf.git'
            }
        }

        stage('Build & Deploy') {
            steps {
                bat '''
                docker-compose down
                docker-compose up -d --build
                '''
            }
        }

    }
}